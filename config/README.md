# Configuration Examples

This directory is reserved for checked-in storage service configuration examples.

Keep examples here focused on:

- standalone `storage-api` processes;
- queue and stream service configuration where those services need files;
- backend selection for SQLite, Turso, Postgres, RocksDB, and FoundationDB;
- local development and container examples used by this repository's tests.

Do not add downstream application configuration here.
